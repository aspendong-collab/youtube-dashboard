# Streamlit Secrets 配置说明

## 概述

本应用使用 YouTube Data API v3 获取视频数据。为了安全地存储 API Key，我们使用 Streamlit Secrets 功能。

## 配置步骤

### 1. 获取 YouTube API Key

1. 访问 [Google Cloud Console](https://console.cloud.google.com/)
2. 创建新项目或选择现有项目
3. 启用 YouTube Data API v3
4. 创建凭据 → API 密钥
5. 复制生成的 API Key

### 2. 在 Streamlit Cloud 配置 Secrets

#### 方式 1：通过 Streamlit Cloud 网页界面（推荐）

1. 登录 [Streamlit Cloud](https://share.streamlit.io/)
2. 进入你的应用
3. 点击 **Settings** → **Secrets**
4. 在编辑器中添加以下内容：

```toml
YOUTUBE_API_KEY = "your_youtube_api_key_here"
```

5. 将 `your_youtube_api_key_here` 替换为你的实际 API Key
6. 点击 **Save** 保存

#### 方式 2：通过 Streamlit CLI（本地开发）

1. 在项目根目录创建 `.streamlit` 文件夹
2. 在 `.streamlit` 文件夹中创建 `secrets.toml` 文件
3. 添加以下内容：

```toml
YOUTUBE_API_KEY = "your_youtube_api_key_here"
```

**⚠️ 重要提示：**
- **不要**将 `secrets.toml` 文件提交到 Git
- 将 `.streamlit/secrets.toml` 添加到 `.gitignore`
- Streamlit Cloud 的 Secrets 不会在代码中显示

### 3. 验证配置

配置完成后，在 Streamlit 页面添加一个视频：
1. 访问"视频管理"页面
2. 输入 YouTube 视频地址
3. 点击"添加视频"按钮
4. 如果配置正确，1-2 秒内会显示"✅ 成功添加 1 个视频并获取数据！"

## API 配额说明

### 免费配额
- 每日 10,000 单位
- 单次获取视频信息：1 单位/视频
- 单次获取评论列表：1 单位/请求

### 使用示例
- 添加 10 个视频：消耗 10 单位
- 每日添加 50 个视频：消耗 50 单位
- 剩余配额可用于定时更新和评论获取

### 查看配额使用情况
访问 [Google Cloud Console](https://console.cloud.google.com/apis/dashboard) 查看 API 使用情况

## 常见问题

### Q: 提示"未配置 YouTube API Key"？
**A:** 请检查 Streamlit Cloud 的 Secrets 是否正确配置了 `YOUTUBE_API_KEY`。

### Q: 提示"API quota exceeded"？
**A:** 已超出每日配额限制。可以：
1. 等待第二天配额重置
2. 升级到付费计划
3. 减少视频添加数量

### Q: 提示"未找到视频数据"？
**A:** 可能的原因：
1. 视频ID不正确
2. 视频已被删除或设为私密
3. API Key 无权限访问该视频

### Q: 数据获取速度很慢？
**A:** 正常情况下 1-2 秒内完成。如果慢，可能是：
1. 网络延迟
2. YouTube API 响应慢
3. 批量获取多个视频

## 安全建议

1. **不要**在代码中硬编码 API Key
2. **不要**将 API Key 提交到 Git 仓库
3. **定期**更换 API Key
4. **限制** API Key 的使用范围
5. **监控** API 使用情况

## 技术细节

### 代码中使用 API Key
```python
import streamlit as st

# 从 Secrets 获取 API Key
api_key = st.secrets["YOUTUBE_API_KEY"]

# 使用 API Key
url = f"https://www.googleapis.com/youtube/v3/videos?key={api_key}"
```

### 本地开发
```python
# 本地开发时，会从 .streamlit/secrets.toml 读取
# Streamlit Cloud 部署后，会从云端 Secrets 读取
api_key = st.secrets["YOUTUBE_API_KEY"]
```

## 更新日志

### v2.0.0 (2026-01-29)
- ✨ 新增实时获取数据功能
- ✨ 使用 Streamlit Secrets 存储 API Key
- 🔒 提升安全性
- ⚡ 优化用户体验，无需手动刷新页面
